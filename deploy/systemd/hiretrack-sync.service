[Unit]
Description=HireTrack incremental sync
Wants=docker.service network-online.target
After=docker.service network-online.target

[Service]
Type=oneshot
WorkingDirectory=/opt/hiretrack-sync/deploy
ExecStart=/usr/bin/docker compose run --rm sync-client
TimeoutStartSec=12h

