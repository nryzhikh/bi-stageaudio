---
- name: Install base apt packages
  ansible.builtin.apt:
    name: "{{ hiretrack_bootstrap_packages }}"
    state: present
    update_cache: true

- name: Create deployment root
  ansible.builtin.file:
    path: "{{ hiretrack_deploy_root }}"
    state: directory
    owner: "{{ hiretrack_owner }}"
    group: "{{ hiretrack_group }}"
    mode: "0755"

- name: Create deploy data directories
  ansible.builtin.file:
    path: "{{ hiretrack_deploy_root }}/{{ item }}"
    state: directory
    owner: "{{ hiretrack_owner }}"
    group: "{{ hiretrack_group }}"
    mode: "0755"
  loop:
    - deploy
    - deploy/caddy_config
    - var
    - var/artifacts

- name: Allow public TCP ports through UFW
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
  loop: "{{ hiretrack_public_tcp_ports }}"

- name: Enable UFW
  community.general.ufw:
    state: enabled
    policy: deny
    direction: incoming

